import { Copyright, Socials } from "../components";

const Footer = () => {
  return (
    <footer className="md:py-12 py-8 flex items-center justify-between w-full">
      <Copyright />
      <Socials />
    </footer>
  );
};

export default Footer;
